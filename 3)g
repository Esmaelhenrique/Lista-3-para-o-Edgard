#include <stdio.h>
#include <math.h>

int main()
{
    int base = 3;

    for (int expoente = 0; expoente <= 15; expoente++)
        {
        long long resultado = pow(base, expoente);

        printf("3^%d = %lld\n", expoente, resultado);
}
}
